// define size of array of lights

/* array of lights (4 strings, 24 lights each, 1 dimension) */
#define SX 4
#define SY 24
#define SZ 1

// maplen should be at least longest dimension * 2
#define MAPLEN 64

//#define BSD         // if BSD
#define WIN       // if Windows

#define LITTLEENDIAN  // for intel, comment out for powerpc
